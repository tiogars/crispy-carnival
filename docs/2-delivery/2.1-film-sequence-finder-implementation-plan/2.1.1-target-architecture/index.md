# 2.1.1 Target Architecture

```text
React + Vite + TypeScript + Remotion
            |
            v
        FastAPI
            |
            v
      Filesystem film library
```

The frontend consumes JSON endpoints and media URLs from FastAPI.
